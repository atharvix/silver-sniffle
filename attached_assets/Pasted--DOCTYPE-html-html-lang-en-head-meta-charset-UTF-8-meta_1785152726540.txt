<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Series – Find your people on iMessage</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
html, body {
  width: 100%;
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  background: #1a0a06 url('cafe-bg.png') center center / cover no-repeat fixed;
  color: #fff;
  overflow-x: hidden;
}
a { text-decoration: none; color: inherit; }
/* ── HERO ── */
.hero {
  position: relative;
  width: 100vw;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}
.hero-bg {
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, rgba(0,0,0,0.30) 0%, rgba(0,0,0,0.08) 40%, rgba(0,0,0,0.30) 100%);
  z-index: 0;
}
/* NAVBAR */
.navbar {
  position: relative;
  z-index: 10;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px 52px;
  width: 100%;
}
.nav-logo {
  font-size: 26px;
  font-weight: 900;
  letter-spacing: -1px;
  color: #fff;
  flex-shrink: 0;
  cursor: pointer;
}
.nav-logo span { font-weight: 200; }
.nav-press {
  display: flex;
  align-items: center;
  gap: 38px;
  flex: 1;
  justify-content: center;
}
.press-bi {
  font-size: 10.5px;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: rgba(255,255,255,0.75);
  line-height: 1.25;
  text-align: center;
}
.press-forbes {
  font-size: 20px;
  font-weight: 700;
  font-family: Georgia, serif;
  color: rgba(255,255,255,0.75);
}
.press-wsj {
  font-size: 20px;
  font-weight: 700;
  font-family: Georgia, serif;
  color: rgba(255,255,255,0.75);
  letter-spacing: -0.5px;
}
.press-entrepreneur {
  font-size: 14px;
  font-weight: 500;
  color: rgba(255,255,255,0.75);
  letter-spacing: 0.01em;
}
.press-complex {
  font-size: 14px;
  font-weight: 900;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: rgba(255,255,255,0.75);
}
.nav-btns {
  display: flex;
  gap: 10px;
  flex-shrink: 0;
}
.btn-login {
  padding: 10px 26px;
  background: rgba(255,255,255,0.1);
  border: 1px solid rgba(255,255,255,0.28);
  border-radius: 8px;
  font-size: 15px;
  font-weight: 500;
  color: #fff;
  cursor: pointer;
  font-family: inherit;
}
.btn-signup {
  padding: 10px 26px;
  background: #fff;
  border: none;
  border-radius: 8px;
  font-size: 15px;
  font-weight: 600;
  color: #111;
  cursor: pointer;
  font-family: inherit;
}
/* MAIN CONTENT */
.main-row {
  position: relative;
  z-index: 5;
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 52px 180px;
  gap: 40px;
  flex-wrap: wrap;
}
.hero-headline {
  font-size: clamp(58px, 6vw, 88px);
  font-weight: 900;
  line-height: 1.0;
  letter-spacing: -3px;
  color: #fff;
  max-width: 55%;
  align-self: flex-start;
  margin-top: 20px;
}
.hero-right {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 16px;
  flex-shrink: 0;
  margin-top: 280px;
}
.cta-pill {
  display: flex;
  align-items: center;
  gap: 14px;
  background: #fff;
  border-radius: 14px;
  padding: 16px 32px;
  cursor: pointer;
  box-shadow: 0 4px 28px rgba(0,0,0,0.4);
  white-space: nowrap;
  border: none;
}
.cta-dot {
  width: 32px;
  height: 32px;
  background: #30d158;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.cta-dot svg { width: 18px; height: 18px; fill: #fff; }
.cta-text {
  font-size: 22px;
  font-weight: 700;
  color: #111;
  letter-spacing: -0.4px;
}
.cta-subtext {
  font-size: 15px;
  color: rgba(255,255,255,0.72);
  line-height: 1.55;
  font-weight: 400;
  max-width: 320px;
}
/* ── FOOTER ── */
.footer {
  position: relative;
  z-index: 5;
  width: 100%;
  background: linear-gradient(to bottom, rgba(0,0,0,0.55) 0%, rgba(0,0,0,0.75) 100%);
  padding: 52px 52px 40px;
}
.footer-top {
  margin-bottom: 28px;
}
.footer-logo {
  font-size: 42px;
  font-weight: 900;
  letter-spacing: -1.5px;
  color: #fff;
  margin-bottom: 14px;
}
.footer-social {
  display: flex;
  align-items: center;
  gap: 18px;
}
.social-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: rgba(255,255,255,0.08);
  border: 1px solid rgba(255,255,255,0.15);
  cursor: pointer;
  transition: background 0.2s ease, border-color 0.2s ease;
}
.social-icon:hover {
  background: rgba(255,255,255,0.16);
  border-color: rgba(255,255,255,0.3);
}
.social-icon svg {
  width: 17px;
  height: 17px;
  fill: rgba(255,255,255,0.85);
}
.footer-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-top: 28px;
  margin-top: 8px;
  border-top: 1px solid rgba(255,255,255,0.08);
  flex-wrap: wrap;
  gap: 16px;
}
.footer-copyright {
  font-size: 13px;
  color: rgba(255,255,255,0.45);
}
.footer-legal {
  display: flex;
  gap: 28px;
  flex-wrap: wrap;
}
.footer-legal a {
  font-size: 13px;
  color: rgba(255,255,255,0.55);
  transition: color 0.2s ease;
}
.footer-legal a:hover { color: rgba(255,255,255,0.9); }
/* ── RESPONSIVE ── */
@media (max-width: 900px) {
  .nav-press { display: none; }
  .hero-headline { max-width: 100%; }
  .main-row { padding: 0 24px 40px; }
  .navbar { padding: 18px 24px; }
  .footer { padding: 40px 24px 28px; }
}
@media (max-width: 560px) {
  .btn-login, .btn-signup { padding: 9px 16px; font-size: 14px; }
  .cta-pill { padding: 14px 24px; }
  .cta-text { font-size: 19px; }
  .footer-bottom { flex-direction: column; align-items: flex-start; }
}
</style>
</head>
<body>
<div class="hero">
  <div class="hero-bg"></div>
  <nav class="navbar">
    <div class="nav-logo">series<span>_</span></div>
    <div class="nav-btns">
      <button class="btn-login">Log In</button>
      <button class="btn-signup">Sign Up</button>
    </div>
  </nav>
  <div class="main-row">
    <div class="hero-headline">See who's<br>around you</div>
    <div class="hero-right">
      <button class="cta-pill">
        <span class="cta-dot">
          <svg viewBox="0 0 24 24"><path d="M20 2H4c-1.1 0-2 .9-2 2v18l4-4h14c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2z"/></svg>
        </span>
        <span class="cta-text">Start Connecting</span>
      </button>
      <p class="cta-subtext">Not another social app.<br>The people here are actually near you.</p>
    </div>
  </div>
</div>
<footer class="footer">
  <div class="footer-top">
    <div class="footer-logo">series<span style="font-weight:200;">_</span></div>
  </div>
  <div class="footer-bottom">
    <div class="footer-copyright">© 2026 Series. All rights reserved.</div>
    <div class="footer-legal">
      <a href="#">Privacy Policy</a>
      <a href="#">Terms of Service</a>
      <a href="#">Contact</a>
    </div>
  </div>
</footer>
</body>
</html>